# NICARA Frontend Environment Variables
# Copy this to .env.local and update values

# Backend API root — must include the /api prefix.
# Local development (Django on port 8000):
NEXT_PUBLIC_API_URL=http://localhost:8000/api
